// type User = { id: string; avatar: string; name: string }; //type alias || type alias can be used for any type of data like string, number, boolean, array, object, etc. but not for function and class. It is more
export interface User {
  id: string;
  avatar: string;
  name: string;
} //interface || interface only for objects
